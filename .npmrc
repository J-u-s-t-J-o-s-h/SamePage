# Fail installs if the declared packageManager/engine does not match.
engine-strict=false
# Keep the dependency tree strict: packages must declare what they import.
# (This is pnpm's default; stated explicitly so the intent is documented.)
auto-install-peers=true
